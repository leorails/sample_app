<%= form_for current_user.relationships.find_by_followed_id(@user),
             :html => { :method => :delete } do |f| %>  <!--  :remote => true  # Listing 12.34  Ajax enabled -->
  <div class="actions"><%= f.submit "Unfollow" %></div>
<% end %>
